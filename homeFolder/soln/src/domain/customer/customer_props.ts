/**
 * Interface for the Customer value object. Contains the properties of Customer
 *
 * @export
 * @interface ICustomerProps
 */
export interface ICustomerProps {
    customerId: string;
    name: string;
    email: string;
}
