/**
 * Interface for the User value object. Contains the properties of User
 *
 * @export
 * @interface IUserProps
 */
export interface IUserProps {
    firstName: string;
    lastName: string;
    email: string;
    phoneNumber?: string;
}
